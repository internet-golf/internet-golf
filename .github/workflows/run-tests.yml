name: Run Tests

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  deploy-site:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: go test ./... -json | go tool tparse -all
